{
  "name": "global-news",
  "version": "2.0.0",
  "description": "Global News Aggregator — RSS + AI classification → GitHub Pages",
  "scripts": {
    "build": "node scripts/build-news.js",
    "dev":   "node scripts/build-news.js && npx serve public"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "node-cache": "^5.1.2",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
